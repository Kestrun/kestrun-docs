---
layout: default
title: "GlobalStore class"
parent: "Kestrun.SharedState"
grand_parent: "C# API"
---
# GlobalStore class

Thread‑safe, case‑insensitive global key/value store for reference‑type objects.

```csharp
public static class GlobalStore
```

## Public Members

| name | description |
| --- | --- |
| static [Count](GlobalStore/Count) { get; } | Gets the number of key/value pairs in the shared state. |
| static [Keys](GlobalStore/Keys) { get; } | Gets an enumerable collection of all keys in the shared state. |
| static [Values](GlobalStore/Values) { get; } | Gets an enumerable collection of all values in the shared state. |
| static [Clear](GlobalStore/Clear)() | Clears all entries in the shared state. |
| static [Contains](GlobalStore/Contains)(…) | Checks if a variable with the specified name exists in the shared state. |
| static [Get](GlobalStore/Get)(…) | Untyped fetch; `null` if absent. |
| static [KeySnapshot](GlobalStore/KeySnapshot)() | Snapshot of keys only—handy for quick listings. |
| static [Remove](GlobalStore/Remove)(…) | Attempts to remove the value with the specified name from the shared state. |
| static [Set](GlobalStore/Set)(…) | Add or overwrite a value (reference‑types only). |
| static [Snapshot](GlobalStore/Snapshot)() | Snapshot of *all* current variables (shallow copy). |
| static [TryGet&lt;T&gt;](GlobalStore/TryGet)(…) | Strongly‑typed fetch. Returns `false` if the key is missing or the stored value can’t be cast to *T*. |

## See Also

* namespace [Kestrun.SharedState](../Kestrun)
* [GlobalStore.cs](https://github.com/Kestrun/Kestrun/tree/main/src/CSharp/Kestrun/SharedState/GlobalStore.cs)

<!-- DO NOT EDIT: generated by xmldo for Kestrun.dll -->
