---
layout: default
title: "SharedStateStore class"
parent: "Kestrun.SharedState"
grand_parent: "C# API"
---
# SharedStateStore class

Thread‑safe, case‑insensitive global key/value store for reference‑type objects.

```csharp
public static class SharedStateStore
```

## Public Members

| name | description |
| --- | --- |
| static [Contains](SharedStateStore/Contains)(…) | Checks if a variable with the specified name exists in the shared state. |
| static [Get](SharedStateStore/Get)(…) | Untyped fetch; `null` if absent. |
| static [KeySnapshot](SharedStateStore/KeySnapshot)() | Snapshot of keys only—handy for quick listings. |
| static [Set](SharedStateStore/Set)(…) | Add or overwrite a value (reference‑types only). |
| static [Snapshot](SharedStateStore/Snapshot)() | Snapshot of *all* current variables (shallow copy). |
| static [TryGet&lt;T&gt;](SharedStateStore/TryGet)(…) | Strongly‑typed fetch. Returns `false` if the key is missing or the stored value can’t be cast to *T*. |

## See Also

* namespace [Kestrun.SharedState](../Kestrun)

<!-- DO NOT EDIT: generated by xmldo for Kestrun.dll -->
